<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClassMapper">
    <select id="list">
        select clazz.id as id,
        clazz.name as name,
        clazz.room as room,
        clazz.begin_date as beginDate,
        clazz.end_date as endDate,
        clazz.master_id as masterId,
        clazz.create_time as createTime,
        clazz.update_time as updateTime,
        (case
        <![CDATA[
           when clazz.begin_date is not null and clazz.begin_date > now() then '未开班'
           when clazz.end_date is not null and clazz.end_date < now() then '已结课'
           else '已开班'
        ]]>
        end) as status,
        emp.name as masterName from  emp right join clazz  on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != ''"> clazz.name like concat('%',#{name},'%') </if>
            <if test="begin != null and end != null"> and clazz.create_time between #{begin} and #{end} </if>
        </where>
        order by clazz.update_time desc
    </select>
    <update id="update">
        update clazz
        <set><!--该标签自动生成set关键字，并且可以自动删除更新字段后的多余-->
            <if test="name != null and name.trim() != ''">
                name= #{name},
            </if>
            <if test="room != null and room.trim()!=''">
                room = #{room},
            </if>
            <if test="beginDate != null and beginDate.trim() != ''">
                begin_date = #{beginDate},
            </if>
            <if test="endDate != null and endDate.trim() != ''">
                end_date = #{endDate},
            </if>
            <if test="masterId != null">
                master_id = #{masterId},
            </if>
            <if test="subject != null">
                subject = #{subject},
            </if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>

</mapper>